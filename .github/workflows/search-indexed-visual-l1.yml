name: Search Indexed Visual L1-L2

on:
  push:
    branches:
      - feat/search-indexed-visual-l1
    paths:
      - "components/search/IndexedTransactionArtwork.tsx"
      - "components/search/SearchListingCardDark.tsx"
      - "lib/ux/indexed-transaction-visual.ts"
      - "lib/ux/indexed-listing-visual-policy.ts"
      - "scripts/scrapers/__tests__/indexed-transaction-visual.test.ts"
      - "scripts/scrapers/__tests__/indexed-listing-visual-policy.test.ts"
      - ".github/workflows/search-indexed-visual-l1.yml"

permissions:
  contents: read

jobs:
  verify:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Test indexed visual contracts
        run: npx tsx --test scripts/scrapers/__tests__/indexed-transaction-visual.test.ts scripts/scrapers/__tests__/indexed-listing-visual-policy.test.ts

      - name: TypeScript
        run: npx tsc --noEmit