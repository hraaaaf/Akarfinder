# P1 Intent Hubs implementation note

Public routes `/acheter`, `/louer`, and `/neuf` now use canonical lightweight hub shells. They no longer duplicate Search filters, fake favorites, static market tables, or a fictional active-new-build catalogue.

Seller structured-draft work remains intentionally separate and open.
