name: Morocco Web L2 Sarouty Adapter

on:
  workflow_dispatch:
  push:
    branches:
      - feat/morocco-web-l2-sarouty-adapter
    paths:
      - 'scripts/acquisition/sarouty-public-adapter.mjs'
      - 'scripts/acquisition/__tests__/sarouty-public-adapter.test.mjs'
      - '.github/workflows/morocco-web-l2-sarouty-adapter.yml'

permissions:
  contents: read

concurrency:
  group: morocco-web-l2-sarouty-${{ github.ref }}
  cancel-in-progress: true

jobs:
  certify:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: '22'

      - name: Unit tests
        run: node --test scripts/acquisition/__tests__/sarouty-public-adapter.test.mjs

      - name: Live public dry-run
        run: node scripts/acquisition/sarouty-public-adapter.mjs

      - name: Publish evidence summary
        if: always()
        run: |
          if [ -f artifacts/morocco-web-l2-sarouty/report.md ]; then
            cat artifacts/morocco-web-l2-sarouty/report.md >> "$GITHUB_STEP_SUMMARY"
          fi

      - name: Upload evidence
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: morocco-web-l2-sarouty-${{ github.run_id }}
          path: artifacts/morocco-web-l2-sarouty/
          if-no-files-found: error
          retention-days: 14
