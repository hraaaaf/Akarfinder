name: ODM Mubawab Detail Geo Precision V1

on:
  pull_request:
    paths:
      - 'supabase/migrations/20260801014500_odm_mubawab_detail_geo_precision_v1.sql'
      - 'scripts/odm-mubawab-detail-geo-precision-v1-contract.mjs'
      - '.github/workflows/odm-mubawab-detail-geo-precision-v1.yml'

permissions:
  contents: read

jobs:
  contract:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
      - run: npm ci
      - name: Mubawab detail geo precision contract
        run: node scripts/odm-mubawab-detail-geo-precision-v1-contract.mjs
      - name: TypeScript
        run: npx tsc --noEmit
      - name: Production build
        run: npm run build
