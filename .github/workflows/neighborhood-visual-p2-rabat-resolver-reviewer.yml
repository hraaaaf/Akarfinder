name: Neighborhood Visual P2 Rabat Resolver Reviewer
on:
  pull_request:
    paths:
      - "lib/contextual-illustrations/rabat-real-photo-library.ts"
      - "components/search/RabatNeighborhoodResolverP2QAFixture.tsx"
      - "app/visual-qa/rabat-neighborhood-resolver-p2/**"
      - "scripts/audits/neighborhood-visual-p2-rabat-resolver-visual-qa.mjs"
      - ".github/workflows/neighborhood-visual-p2-rabat-resolver-reviewer.yml"
permissions: { contents: read }
env: { NEIGHBORHOOD_VISUAL_QA: "1", NEXT_PUBLIC_SEARCH_GATEWAY_ENABLED: "false" }
jobs:
  reviewer:
    name: Product Design Reviewer — P2 Rabat resolver
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with: { node-version: 24, cache: npm }
      - run: npm ci
      - run: npx playwright install --with-deps chromium
      - run: npm run build
      - run: |
          npm run start -- -p 3191 > /tmp/p2-rabat-reviewer.log 2>&1 &
          for i in {1..40}; do if curl -fsS http://127.0.0.1:3191/visual-qa/rabat-neighborhood-resolver-p2 >/dev/null; then exit 0; fi; sleep 1; done
          cat /tmp/p2-rabat-reviewer.log; exit 1
      - env: { BASE_URL: "http://127.0.0.1:3191", AUDIT_VARIANT: product-design }
        run: node scripts/audits/neighborhood-visual-p2-rabat-resolver-visual-qa.mjs
      - uses: actions/upload-artifact@v4
        if: always()
        with: { name: neighborhood-visual-p2-rabat-resolver-product-design-proof, path: data/audits/neighborhood-visual-p2-rabat-resolver/product-design, if-no-files-found: error, retention-days: 14 }
