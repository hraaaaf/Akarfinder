name: TMP P1B.6 Docs Closeout

on:
  push:
    branches:
      - docs/p1b6-closeout
    paths:
      - scripts/tmp-p1b6-docs-closeout.py
      - .github/workflows/tmp-p1b6-docs-closeout.yml

permissions:
  contents: write

jobs:
  closeout:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout branch
        uses: actions/checkout@v4
        with:
          ref: docs/p1b6-closeout
      - name: Apply P1B.6 canonical docs closeout
        run: python scripts/tmp-p1b6-docs-closeout.py
      - name: Self-clean and commit canonical docs
        run: |
          git config user.name github-actions[bot]
          git config user.email 41898282+github-actions[bot]@users.noreply.github.com
          git rm scripts/tmp-p1b6-docs-closeout.py .github/workflows/tmp-p1b6-docs-closeout.yml
          git add README.md docs/ROADMAP.md docs/SESSION.md
          git diff --cached --name-only
          git commit -m "docs: close P1B.6 geo coverage depth audit"
          git push origin HEAD:docs/p1b6-closeout
