name: MON PROJET P1A Visual Certification

on:
  push:
    branches:
      - ux/mon-projet-p1a
    paths:
      - .github/workflows/mon-projet-p1a-visual-certification.yml
      - app/mon-projet/**
      - app/compagnon/**
      - components/companion/**

permissions:
  contents: read

jobs:
  certify:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 24
          cache: npm
      - run: npm ci
      - run: npx playwright install --with-deps chromium
      - run: npm run build
      - name: Start production server
        run: |
          npm run start > /tmp/akarfinder-mon-projet.log 2>&1 &
          for i in {1..60}; do
            if curl -fsS http://127.0.0.1:3000/mon-projet >/dev/null; then exit 0; fi
            sleep 1
          done
          cat /tmp/akarfinder-mon-projet.log
          exit 1
      - name: Capture and inspect
        run: |
          mkdir -p artifacts/mon-projet-p1a
          node <<'NODE'
          const { chromium } = require('playwright');
          const fs = require('fs');
          (async () => {
            const browser = await chromium.launch({ headless: true });
            const viewports = [
              { name: '390', width: 390, height: 844 },
              { name: '768', width: 768, height: 1024 },
              { name: '1280', width: 1280, height: 900 },
            ];
            const report = [];
            for (const viewport of viewports) {
              const page = await browser.newPage({ viewport: { width: viewport.width, height: viewport.height } });
              await page.goto('http://127.0.0.1:3000/mon-projet', { waitUntil: 'networkidle' });
              await page.screenshot({ path: `artifacts/mon-projet-p1a/mon-projet-${viewport.name}.png`, fullPage: true });
              const metrics = await page.evaluate(() => ({
                h1: document.querySelectorAll('h1').length,
                main: document.querySelectorAll('main').length,
                scrollWidth: document.documentElement.scrollWidth,
                clientWidth: document.documentElement.clientWidth,
                height: document.documentElement.scrollHeight,
                duplicateIds: [...document.querySelectorAll('[id]')].map(n => n.id).filter((id, i, a) => a.indexOf(id) !== i),
                unnamedLinks: [...document.querySelectorAll('a')].filter(a => !(a.textContent || '').trim() && !a.getAttribute('aria-label')).length,
                unnamedButtons: [...document.querySelectorAll('button')].filter(b => !(b.textContent || '').trim() && !b.getAttribute('aria-label')).length,
              }));
              report.push({ viewport, ...metrics, overflow: metrics.scrollWidth - metrics.clientWidth });
              await page.close();
            }
            fs.writeFileSync('artifacts/mon-projet-p1a/report.json', JSON.stringify(report, null, 2));
            await browser.close();
            if (report.some(r => r.overflow > 0 || r.h1 !== 1 || r.main !== 1 || r.duplicateIds.length || r.unnamedLinks || r.unnamedButtons)) process.exit(1);
          })();
          NODE
      - uses: actions/upload-artifact@v4
        with:
          name: mon-projet-p1a-visual-certification
          path: artifacts/mon-projet-p1a
          if-no-files-found: error
