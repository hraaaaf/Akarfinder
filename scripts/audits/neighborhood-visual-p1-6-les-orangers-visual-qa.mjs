import { chromium } from "playwright";
import { mkdir, writeFile } from "node:fs/promises";
const baseUrl=process.env.BASE_URL??"http://127.0.0.1:3000"; const variant=process.env.AUDIT_VARIANT??"local"; const outputDir=`data/audits/neighborhood-visual-p1-6-les-orangers/${variant}`;
const viewports=[{name:"mobile-360x800",width:360,height:800,imageHeight:164,detailCount:2},{name:"mobile-390x844",width:390,height:844,imageHeight:164,detailCount:2},{name:"tablet-768x900",width:768,height:900,imageHeight:196,detailCount:3},{name:"desktop-1024x800",width:1024,height:800,imageHeight:196,detailCount:3},{name:"desktop-1280x900",width:1280,height:900,imageHeight:196,detailCount:3},{name:"desktop-1440x900",width:1440,height:900,imageHeight:196,detailCount:3}];
const expectedRoles=["signature","immobilier","lifestyle"]; const expectedIds=["rabat-les-orangers-signature-v1","rabat-les-orangers-immobilier-v1","rabat-les-orangers-lifestyle-v1"];
const expectedSourceDimensions=[{path:"/__qa/les-orangers-signature.jpg",width:2048,height:1375},{path:"/__qa/les-orangers-immobilier.jpg",width:1280,height:720},{path:"/__qa/les-orangers-lifestyle.jpg",width:1280,height:720}];
await mkdir(outputDir,{recursive:true}); const browser=await chromium.launch({headless:true}); const results=[]; let failure=null;
try{for(const viewport of viewports){const page=await browser.newPage({viewport:{width:viewport.width,height:viewport.height}});try{const response=await page.goto(`${baseUrl}/visual-qa/les-orangers`,{waitUntil:"networkidle",timeout:45000});if(!response||response.status()>=400)throw new Error(`${viewport.name}: QA route returned ${response?.status()}`);await page.waitForSelector("[data-les-orangers-visual-qa-grid] [data-les-orangers-qa-card]",{timeout:15000});const decoded=await page.evaluate(async(sources)=>Promise.all(sources.map(async s=>{const i=new Image();i.src=s.path;await i.decode();return{path:s.path,width:i.naturalWidth,height:i.naturalHeight}})),expectedSourceDimensions);for(const e of expectedSourceDimensions){const d=decoded.find(x=>x.path===e.path);if(!d||d.width!==e.width||d.height!==e.height)throw new Error(`${viewport.name}: source dimension drift ${e.path}`)}const metrics=await page.evaluate(()=>{const visible=(node)=>Boolean(node&&getComputedStyle(node).display!=="none"&&getComputedStyle(node).visibility!=="hidden"&&node.getBoundingClientRect().width>0&&node.getBoundingClientRect().height>0);const text=(node)=>(node?.textContent??"").replace(/\s+/g," ").trim();const cards=[...document.querySelectorAll("[data-les-orangers-qa-card]")];return{clientWidth:document.documentElement.clientWidth,scrollWidth:document.documentElement.scrollWidth,cards:cards.map(card=>{const frame=card.querySelector("[data-card-image]");const m=card.querySelector("[data-neighborhood-template-a-descriptors-mobile]");const d=card.querySelector("[data-neighborhood-template-a-descriptors-desktop]");const active=visible(m)?m:visible(d)?d:null;const dt=text(active);return{role:card.getAttribute("data-scene-role"),id:card.getAttribute("data-visual-id"),imageHeight:frame?.getBoundingClientRect().height??0,title:text(card.querySelector("[data-neighborhood-template-a-title]")),city:text(card.querySelector("[data-neighborhood-template-a-title]")?.nextElementSibling),detailCount:dt?dt.split("•").length:0,disclosure:text(card.querySelector("[data-neighborhood-template-a-disclosure]")),credit:text(card.querySelector("[data-les-orangers-qa-credit]")),background:frame?getComputedStyle(frame,"::before").backgroundImage:""}})}});if(metrics.cards.length!==3)throw new Error(`${viewport.name}: expected 3 cards`);if(metrics.scrollWidth>metrics.clientWidth+1)throw new Error(`${viewport.name}: horizontal overflow`);if(JSON.stringify(metrics.cards.map(c=>c.role))!==JSON.stringify(expectedRoles))throw new Error(`${viewport.name}: role drift`);if(JSON.stringify(metrics.cards.map(c=>c.id))!==JSON.stringify(expectedIds))throw new Error(`${viewport.name}: id drift`);if(new Set(metrics.cards.map(c=>c.background)).size!==3)throw new Error(`${viewport.name}: backgrounds not distinct`);for(const card of metrics.cards){if(Math.abs(card.imageHeight-viewport.imageHeight)>1.5)throw new Error(`${viewport.name}/${card.role}: image height drift`);if(card.title!=="LES ORANGERS"||card.city!=="Rabat")throw new Error(`${viewport.name}/${card.role}: identity drift`);if(card.detailCount!==viewport.detailCount)throw new Error(`${viewport.name}/${card.role}: detail count drift`);if(card.disclosure!=="Photo d’ambiance")throw new Error(`${viewport.name}/${card.role}: disclosure drift`);if(!/CC BY/.test(card.credit))throw new Error(`${viewport.name}/${card.role}: credit missing`)}await page.screenshot({path:`${outputDir}/${viewport.name}.png`,fullPage:true});results.push({viewport:viewport.name,machine_quality_score:10,horizontal_overflow:false,duplicate_labels:false})}catch(error){failure=error;break}finally{await page.close()}}}finally{await browser.close()}
await writeFile(`${outputDir}/metrics.json`,`${JSON.stringify({generated_at:new Date().toISOString(),variant,target_score:9,results,failure:failure instanceof Error?failure.message:null},null,2)}\n`);if(failure)throw failure;
