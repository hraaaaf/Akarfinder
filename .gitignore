# Dependencies
node_modules/
.pnp
.pnp.js

# Build output
.next/
out/
dist/
build/

# Environment secrets — NEVER commit
.env
.env.local
.env.*.local
.env.production
.env.development

# Vercel
.vercel/

# SQLite database files (but keep akarfinder.db production database)
db/*.db
db/*.db-shm
db/*.db-wal

# Server logs
*.log
build-server*.log
dev-server.log

# Documentation screenshots (large PNGs, not app assets)
public/screenshots/

# Debug/QA screenshots at project root (dev artifacts)
/*.png
/*.jpg
/*.patch
/.next-dev.err
/.api-listings.json
/STATE.md
/AGENTS.md

# Partner CSV imports (real data, not to be committed)
data/imports/*.csv
data/imports/*.xlsx
data/imports/reports/*.json

# OS
.DS_Store
Thumbs.db

# TypeScript
*.tsbuildinfo
next-env.d.ts

.vercel

# Local AI tooling (UI UX Pro Max skills + local Claude settings) — not app code
.claude/
